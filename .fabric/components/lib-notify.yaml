id: lib/notify.sh
name: notify
type: script
subsystem: framework-core
location: lib/notify.sh
tags: [ntfy, notifications, alerts]

purpose: "Push notification wrapper — fw_notify() function sends alerts via skills-manager alert dispatcher. Fire-and-forget, opt-in via .context/notify-config.yaml. Used by check-tier0.sh, update-task.sh, audit.sh."

depends_on:
  - target: ../agents/context/check-tier0.sh
    type: calls
  - target: ../agents/task-create/update-task.sh
    type: calls
  - target: ../agents/audit/audit.sh
    type: calls

depended_by:
  - target: ../bin/fw
    type: calls

last_verified: 2026-03-29
created_by: agent
