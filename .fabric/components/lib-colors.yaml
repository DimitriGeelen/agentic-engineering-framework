id: lib/colors.sh
name: colors
type: script
subsystem: framework-core
location: lib/colors.sh
tags: []

purpose: "Terminal color definitions: BOLD, RED, GREEN, YELLOW, CYAN, NC (no color). Sourced by all framework scripts for consistent output."

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-03-11
created_by: unknown
