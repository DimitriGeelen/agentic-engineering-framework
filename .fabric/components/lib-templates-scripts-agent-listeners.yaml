id: lib/templates/scripts/agent-listeners.sh
name: agent-listeners
type: script
subsystem: termlink-integration
location: lib/templates/scripts/agent-listeners.sh
tags: []

purpose: "Template skill script: single-hub agent-presence discovery reader consuming listener-heartbeat convention (T-1833)"

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-09-08
created_by: unknown
