id: agents/context/commit-cadence.sh
name: commit-cadence
type: script
subsystem: context-fabric
location: agents/context/commit-cadence.sh
tags: []

purpose: "PostToolUse hook: monitor time since last commit — warns when commit cadence exceeds threshold (P-009 budget management)."

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-03-27
created_by: unknown
