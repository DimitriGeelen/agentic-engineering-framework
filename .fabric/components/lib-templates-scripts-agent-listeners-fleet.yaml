id: lib/templates/scripts/agent-listeners-fleet.sh
name: agent-listeners-fleet
type: script
subsystem: termlink-integration
location: lib/templates/scripts/agent-listeners-fleet.sh
tags: []

purpose: "Template skill script: cross-hub agent-presence discovery aggregating listener heartbeats across the fleet (T-1837, /peers)"

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-09-08
created_by: unknown
