id: web/templates/_review_error.html
name: _review_error
type: fragment
subsystem: watchtower
location: web/templates/_review_error.html
tags: []

purpose: "htmx partial: review error message — displayed when task review action fails (task not found, validation error)."

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-03-28
created_by: unknown
