id: lib/templates/scripts/recent-dm.sh
name: recent-dm
type: script
subsystem: termlink-integration
location: lib/templates/scripts/recent-dm.sh
tags: []

purpose: "Template skill script: per-peer DM conversation history reader (T-1862, /recent-dm)"

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-09-08
created_by: unknown
