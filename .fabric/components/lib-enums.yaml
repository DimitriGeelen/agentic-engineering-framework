id: lib/enums.sh
name: enums
type: script
subsystem: framework-core
location: lib/enums.sh
tags: [shell, enums, validation, core]

purpose: "Single source of truth for framework enumerations — valid statuses, workflow types, horizons, and status transitions. Provides is_valid_status(), is_valid_type(), is_valid_horizon(), is_valid_transition() functions. Replaces hardcoded lists previously duplicated across 6+ files."

depends_on: []

depended_by:
  - target: agents/task-create/create-task.sh
    type: calls
  - target: agents/task-create/update-task.sh
    type: calls

last_verified: 2026-03-10
created_by: T-413
