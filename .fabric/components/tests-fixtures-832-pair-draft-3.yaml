id: tests/fixtures/832/pair-draft-3.bpmn
name: pair-draft-3
type: data
subsystem: testing
location: tests/fixtures/832/pair-draft-3.bpmn
tags:
- designer
- offpage-seam
- 832
- fixture-pin
purpose: '832''s pair-draft #3 exemplar (their T-219): offpage-seam.bpmn exercising the T-2571 contract-v0 three legs (RESOLVED
  aef-task-lifecycle 1f9b5f0c / GHOST 22222222 / LEGACY review-map slug). Byte-delivered on the DM rail (offsets 120-121),
  sha256-pinned by the .sha256 sibling — f9422acd330d240dec384591753782dde940289cc94475f22be96aa1551d0c5c, raw 10062 B. READ-ONLY:
  any change breaks the cross-agent pin (832 guards the same bytes in their test_corpus_fixture_pins.py). RE-PINNED 2026-08-01
  (T-324, rail 366): prior pin 0bc15bfac81d…b691449d / 10014 B carried three <bpmn:linkEventThrow> host elements — not a BPMN
  element at any spec version. 832 renamed all three to <bpmn:intermediateThrowEvent>, tag-only, six lines, <aef:link> children
  and host id/name byte-identical. Verified by RECONSTRUCTION not by paste: applied the rename locally, sha matched f9422acd exactly.
  NOTE the pin''s scope (OBS-116) — it answers "did these bytes change", never "are these bytes well-formed"; it was green on the
  malformed element for its entire life. Element-vocabulary checking belongs at intake, not here.'
depends_on: []
depended_by:
- target: tests/web/test_pair_draft3_intake.py
  type: reads
- target: tests/web/test_bpmn_frozen_v1_pin.py
  type: read_by
- target: tests/web/test_pair_draft3_intake.py
  type: read_by
- target: tests/web/test_bpmn_frozen_v1_pin.py
  type: called_by
- target: tests/web/test_pair_draft3_intake.py
  type: called_by
last_verified: 2026-07-21
created_by: T-2591
last_enriched: '2026-07-27'
