# Audit Results - 2026-09-25-0920
timestamp: 2026-09-25T07:20:54Z
sections: "structure"
summary:
  pass: 36
  warn: 19
  fail: 0
findings:
  - level: PASS
    check: "Tasks directory exists"
  - level: PASS
    check: "Tasks/active directory exists"
  - level: PASS
    check: "Tasks/completed directory exists"
  - level: PASS
    check: "Tasks/templates directory exists"
  - level: PASS
    check: "Task template exists"
  - level: PASS
    check: "No duplicate task IDs — examined 3444 task file(s) across 1 corpus view(s)"
  - level: PASS
    check: "All project YAML files parse correctly — examined 31 project YAML file(s)"
  - level: PASS
    check: "All arc anchor_task references resolve to existing tasks — examined 19 arc anchor_task reference(s)"
  - level: PASS
    check: "All onboarding-seed corpus references resolve to existing maps — examined 12 onboarding-seed corpus reference(s)"
  - level: PASS
    check: "Corpus maps lint clean — examined 8 corpus map(s)"
  - level: WARN
    check: "Arc 'capability-overlay' has no task commits in the last 30 days (15 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: WARN
    check: "Arc 'horizon-axis-hardening' has no task commits in the last 30 days (5 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: WARN
    check: "Arc 'onboarding-shape-detection' has no task commits in the last 30 days (3 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: WARN
    check: "Arc 'project-shape-resilience' has no task commits in the last 30 days (18 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: WARN
    check: "Arc 'readme-first-run' has no task commits in the last 30 days (2 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: INFO
    check: "free driver F-AUTONOMY: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F3: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F1: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F2: retire_when text present, no recognition heuristic — manual review"
  - level: PASS
    check: "No inline arc:<slug> tag-only scans outside canonical lib (T-1881) — examined 459 source file(s) under lib/ web/ agents/ bin/ tools/"
  - level: PASS
    check: "No PROJECT_ROOT resolution of framework-owned assets (T-2648, OBS-097) — examined 165 Python file(s) under web/ + lib/"
  - level: PASS
    check: "No stale-slice-references (L-417) — examined 307 file(s) under web/templates web/blueprints lib"
  - level: WARN
    check: "Found 185 GO-scope-not-propagated inception(s) of 367 GO-recorded completed inception(s) examined — GO recorded, related_tasks empty, nobody back-references, no unlocks_inception_decision"
    mitigation: "Triage: per inception either backfill related_tasks: / unlocks_inception_decision:, or file the slices its GO approved. Full list: cat /opt/999-Agentic-Engineering-Framework/.context/audits/go-scope-unpropagated/LATEST.md (origin: T-2078, T-2091, T-3099; sibling to L-417/T-1975)"
  - level: PASS
    check: "Fabric: 1337 registered card(s)"
  - level: WARN
    check: "Fabric: 91/1254 cards have no edges"
    mitigation: "Run: fw fabric enrich"
  - level: WARN
    check: "Fabric drift: 1 source file(s) have no fabric card"
    mitigation: "Run: fw fabric scan"
  - level: WARN
    check: "Fabric: 803 card(s) point at files no watch pattern covers"
    mitigation: "Widen .fabric/watch-patterns.yaml, or remove the cards if they are not components"
  - level: PASS
    check: "Cron registry in sync with /etc/cron.d/agentic-audit-999-agentic-engineering-framework"
  - level: PASS
    check: "Watchtower currency: running pid 2028500 is newer than every file under web/"
  - level: PASS
    check: "Exec-bit parity: every 100755-indexed script is executable on disk — examined 61 indexed-100755 script(s) (*.sh + bin/fw)"
  - level: PASS
    check: "Cron exec-bit: every script cron execs directly is executable — examined 3 directly-invoked script(s) in the deployed crontab"
  - level: WARN
    check: "Branch hygiene: 18 finding(s) — stale branches, worktrees or remote refs"
    mitigation: "Cleanup: git branch -d <name> (merged); fw integrate run bleeding-edge (overdue merge-back). Full list: bash -c 'source \"/opt/999-Agentic-Engineering-Framework/lib/branch-hygiene.sh\"; fw_branch_hygiene \"/opt/999-Agentic-Engineering-Framework\"' — FORK present: reconcile while small (merge origin/bleeding-edge INTO the branch, or reset if its commits already landed). Do NOT use fw integrate on a fork."
  - level: PASS
    check: "Hook threshold: no hooks failing over threshold"
  - level: PASS
    check: "Self-vendor drift: vendored .agentic-framework/ in sync with source (libs + templates)"
  - level: PASS
    check: "Invariant suite (tests/lint) green — examined 110 structural invariant(s) (tests/lint/)"
  - level: PASS
    check: "Dead-negation lint (tests/) clean — examined 780 bats file(s) scanned for dead negations (tests/, T-3138/T-3191)"
  - level: WARN
    check: "Unit suite (tests/unit) COULD NOT DETERMINE — nightly run hit its 7200s ceiling (T-3302, OBS-392)"
    mitigation: "Make a run COMPLETE before trusting any count: raise FW_UNIT_SUITE_TIMEOUT, or split/shard the corpus (OBS-388 covers the nested tests/lint suite). Re-running unit-suite.sh unchanged just re-times-out. Until one completes, treat tests/unit as UNKNOWN"
  - level: PASS
    check: "Designer ghost registry: ghost↔task joins clean"
  - level: PASS
    check: "Map conformance: aef-task-lifecycle matches its enforced machine"
  - level: PASS
    check: "Map conformance: aef-inception-flow matches its enforced machine"
  - level: PASS
    check: "Map conformance: aef-dispatch-loop matches its enforced machine"
  - level: PASS
    check: "Map conformance: aef-audit-cron matches its enforced machine"
  - level: PASS
    check: "Map conformance: aef-tier0-escalation matches its enforced machine"
  - level: INFO
    check: "Stale draft(s), 30d+ untouched: draft-arc-lifecycle draft-exception-handling draft-inception-readiness draft-knowledge-leveling draft-t2584-scratch draft-task-creation draft-trigger-handling — promote via the ceremony or delete from the gallery (T-2623)"
  - level: PASS
    check: "Sidecar ledger: 45 consult(s), 45 delivered, 0 in flight, 0 UNKNOWN, 0 dead-lettered, 0 expired-unswept"
  - level: WARN
    check: "BVP driver identity-fidelity has neither a handler nor a scoring spec"
    mitigation: "Give it a mechanism: draft a scoring: spec, check it with 'bin/fw bvp driver --validate-scoring <file>', try it with 'bin/fw bvp driver --explain identity-fidelity T-XXXX --scoring-file <file>'; schema in policy/value-drivers.yaml header (T-3428)"
  - level: WARN
    check: "BVP driver provisioning-safety has neither a handler nor a scoring spec"
    mitigation: "Give it a mechanism: draft a scoring: spec, check it with 'bin/fw bvp driver --validate-scoring <file>', try it with 'bin/fw bvp driver --explain provisioning-safety T-XXXX --scoring-file <file>'; schema in policy/value-drivers.yaml header (T-3428)"
  - level: WARN
    check: "BVP driver Discard fidelity has neither a handler nor a scoring spec"
    mitigation: "Give it a mechanism: draft a scoring: spec, check it with 'bin/fw bvp driver --validate-scoring <file>', try it with 'bin/fw bvp driver --explain Discard fidelity T-XXXX --scoring-file <file>'; schema in policy/value-drivers.yaml header (T-3428)"
  - level: WARN
    check: "BVP driver Loop closure (conditional) has neither a handler nor a scoring spec"
    mitigation: "Give it a mechanism: draft a scoring: spec, check it with 'bin/fw bvp driver --validate-scoring <file>', try it with 'bin/fw bvp driver --explain Loop closure (conditional) T-XXXX --scoring-file <file>'; schema in policy/value-drivers.yaml header (T-3428)"
  - level: WARN
    check: "BVP driver unknown-input-safety has neither a handler nor a scoring spec"
    mitigation: "Give it a mechanism: draft a scoring: spec, check it with 'bin/fw bvp driver --validate-scoring <file>', try it with 'bin/fw bvp driver --explain unknown-input-safety T-XXXX --scoring-file <file>'; schema in policy/value-drivers.yaml header (T-3428)"
  - level: WARN
    check: "BVP driver first-run-recoverability has neither a handler nor a scoring spec"
    mitigation: "Give it a mechanism: draft a scoring: spec, check it with 'bin/fw bvp driver --validate-scoring <file>', try it with 'bin/fw bvp driver --explain first-run-recoverability T-XXXX --scoring-file <file>'; schema in policy/value-drivers.yaml header (T-3428)"
  - level: PASS
    check: "Arc driver reviewer records: 7 scoped driver(s) on in-progress arcs, every reviewer-approved one carries its verdict"
  - level: PASS
    check: "Delegation surface: reviewer-closeable 13, agent-self 1, operator-only 333 across 317 active task(s) with open Human criteria"
  - level: WARN
    check: "Continuous-run wrapper last recorded ARMED but the turn driver is not"
    mitigation: "fw continuous arm --hours N --iterations N --directive '...' (T-3225). Same drift class as the 74-day and 8-day prior incidents (G-099)."
  - level: WARN
    check: "Fabric: 145 under-populated card(s)"
    mitigation: "Run: bin/fw fabric enrich --describe-only (fills purpose/subsystem from each file's own header; names what it refuses). Zero-edge cards want a look, not a re-run: bin/fw fabric drift"
