# Audit Results - 2026-08-16-1423
timestamp: 2026-08-16T12:23:53Z
sections: "compliance"
summary:
  pass: 1
  warn: 51
  fail: 21
findings:
  - level: WARN
    check: "Task T-2720-keystone-onboarding-set-contains-nothing.md missing field: owner"
    mitigation: "Fix missing field: owner in T-2720-keystone-onboarding-set-contains-nothing.md"
  - level: PASS
    check: "CTL-026: Human sovereignty gate present (completion + owner protection)"
  - level: WARN
    check: "CTL-028: T-2411 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2411 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-100194 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-100194 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-100195 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-100195 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2486 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2486 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2487 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2487 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2488 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2488 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2491 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2491 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2492 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2492 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2493 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2493 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2494 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2494 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2495 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2495 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2497 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2497 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2498 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2498 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2509 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2509 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: WARN
    check: "CTL-028: T-2853 is in .tasks/completed/ but frontmatter status='started-work' (expected: work-completed)"
    mitigation: "Fix: bin/fw task update T-2853 --status work-completed — the normal close runs the AC and verification gates and usually passes (832 measured 3 of 4 real drifted tasks closing clean two weeks stale). If a gate legitimately fails, fix the work or re-open it. Only as a last resort: add --force, which BYPASSES those gates and logs a Tier-2 entry. Do not hand-edit frontmatter — that is the git-mv bypass (L-390) this control detects."
  - level: FAIL
    check: "CTL-030: T-1700 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2401 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2405 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2411 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2412 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-100194 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-100195 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2486 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2487 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2488 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2491 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2492 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2493 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2494 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2495 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2497 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2498 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2509 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2439 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2521 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: FAIL
    check: "CTL-030: T-2853 is in .tasks/completed/ but stored horizon='now' (expected: null/absent — render derives 'past' from _location, T-2160)"
    mitigation: "Fix: bin/migrate-horizon-null-completed.sh   (idempotent, only touches completed/ files with non-null horizon)"
  - level: WARN
    check: "CTL-029: T-100201 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-100201 --status work-completed"
  - level: WARN
    check: "CTL-029: T-1062 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-1062 --status work-completed"
  - level: WARN
    check: "CTL-029: T-1274 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-1274 --status work-completed"
  - level: WARN
    check: "CTL-029: T-1542 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-1542 --status work-completed"
  - level: WARN
    check: "CTL-029: T-1624 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-1624 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2171 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2171 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2200 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2200 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2202 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2202 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2205 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2205 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2219 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2219 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2221 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2221 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2268 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2268 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2306 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2306 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2309 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2309 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2410 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2410 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2420 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2420 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2715 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2715 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2716 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2716 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2720 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2720 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2801 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2801 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2802 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2802 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2871 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2871 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2876 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2876 --status work-completed"
  - level: WARN
    check: "CTL-029: T-2969 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-2969 --status work-completed"
  - level: WARN
    check: "CTL-029: T-332 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-332 --status work-completed"
  - level: WARN
    check: "CTL-029: T-334 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-334 --status work-completed"
  - level: WARN
    check: "CTL-029: T-464 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-464 --status work-completed"
  - level: WARN
    check: "CTL-029: T-544 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-544 --status work-completed"
  - level: WARN
    check: "CTL-029: T-801 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-801 --status work-completed"
  - level: WARN
    check: "CTL-029: T-802 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-802 --status work-completed"
  - level: WARN
    check: "CTL-029: T-803 has all Agent ACs ticked but status='started-work' — completable, not closed"
    mitigation: "Run: bin/fw task update T-803 --status work-completed"
  - level: WARN
    check: "CTL-012: Completed task T-436 has unchecked AC"
    mitigation: "Review task completion — AC gate may have been bypassed"
  - level: WARN
    check: "CTL-012: Completed task T-678 has unchecked AC"
    mitigation: "Review task completion — AC gate may have been bypassed"
  - level: WARN
    check: "CTL-012-MISSING-DECIDE: Inception task T-2494 flipped without decide ceremony"
    mitigation: "Auto-tick markers present but ## Decision section empty — run: fw inception decide T-2494 go"
  - level: WARN
    check: "CTL-012: Completed task T-2389 has unchecked AC"
    mitigation: "Review task completion — AC gate may have been bypassed"
