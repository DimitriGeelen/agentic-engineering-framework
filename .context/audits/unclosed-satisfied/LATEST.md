# Unclosed-but-satisfied active tasks

_Generated by `fw audit` (T-3061). Read-only — no task is closed, no
status changed, no box ticked by this scan._

## Criterion

A task in `.tasks/active/` qualifies when: frontmatter `status:` is
`started-work` or `issues`; the Acceptance Criteria section has at least
one Agent AC; every Agent AC is ticked `- [x]`; zero Human AC checkboxes
are left unticked. Lines inside HTML comment blocks (including the task
template's own example ACs) are excluded, so an unedited template never
self-qualifies.

## Limits — read before acting on this list

**A ticked box is a claim, not evidence.** This scan reads checkbox
characters; it does not run the task's `## Verification` block, does not
re-derive whether the described work actually shipped, and cannot tell a
genuinely finished task from one where an agent ticked ahead of itself.
Each row below is a **candidate for close, never a closure** — verify with
`fw task verify T-XXX` or a per-task review before touching
`fw task update --status work-completed`. Never batch-close on this list
alone (CLAUDE.md Human Task Completion Rule).

Rows with an empty `## Verification` block need that scrutiny most —
nothing mechanical would gate their close.

## Qualifying tasks (16)

| Task | Status | Workflow | Name | Agent ACs | Verification cmds? |
|------|--------|----------|------|----------:|--------------------|
| T-1274 | started-work | build | Memory writes (claude auto-memory) blocked by onboar... | 5 | yes |
| T-1542 | started-work | build | fw upgrade from inside a consumer crashes at step 4b... | 6 | yes |
| T-1624 | started-work | build | Refresh ring20-dashboard hub.secret on this anchor —... | 3 | **no** |
| T-2200 | started-work | build | dispatch AEF setup worker on /opt/fan-dashboard | 4 | **no** |
| T-2202 | started-work | build | dispatch AEF setup worker on /opt/832-Workflow-designer | 4 | **no** |
| T-2410 | started-work | build | check-active-task hook: false positives block legiti... | 4 | yes |
| T-2801 | started-work | build | fw init is not atomic and its debris is not fw-recov... | 5 | yes |
| T-2802 | started-work | build | fw watchtower url returns http://localhost:3000 from... | 6 | yes |
| T-2871 | started-work | build | corpus depends 91% on non-frozen aef:meta keys — no ... | 6 | yes |
| T-2876 | started-work | inception | Interpreter-mediated writes bypass the Bash task gat... | 3 | **no** |
| T-2969 | started-work | build | draft arc with all constituents complete is reported... | 5 | yes |
| T-3215 | started-work | build | Port T-1032 next-hint from consumer 002 — post-move ... | 5 | yes |
| T-3268 | started-work | build | continuous-run stale-terminate cycling has no detect... | 5 | yes |
| T-801 | started-work | build | fw costs CLI — token usage tracking from JSONL trans... | 9 | yes |
| T-802 | started-work | build | Watchtower token dashboard — /costs page with sessio... | 7 | yes |
| T-803 | started-work | build | Landing page token widget — show current session tok... | 5 | yes |

