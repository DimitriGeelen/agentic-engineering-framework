# Audit Results - 2026-06-25-1147
timestamp: 2026-06-25T09:47:23Z
sections: "structure"
summary:
  pass: 21
  warn: 3
  fail: 0
findings:
  - level: PASS
    check: "Tasks directory exists"
  - level: PASS
    check: "Tasks/active directory exists"
  - level: PASS
    check: "Tasks/completed directory exists"
  - level: PASS
    check: "Tasks/templates directory exists"
  - level: PASS
    check: "Task template exists"
  - level: PASS
    check: "No duplicate task IDs across active/ and completed/"
  - level: PASS
    check: "All 24 project YAML files parse correctly"
  - level: PASS
    check: "All 12 arc anchor_task references resolve to existing tasks"
  - level: PASS
    check: "All 10 in-progress arc(s) had task commits within 30 days"
  - level: WARN
    check: "free driver F-ORCH: retire_when condition appears met (T-1643 completed cleanly OR G-064 closed) — review whether to retire"
    mitigation: "Review the driver; if truly retired, comment it out or move to candidates section. Silence with FW_RETIRE_WHEN_ADVISORY=0."
  - level: INFO
    check: "free driver F-AUTONOMY: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F3: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F1: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F2: retire_when text present, no recognition heuristic — manual review"
  - level: PASS
    check: "No inline arc:<slug> tag-only scans outside canonical lib (T-1881)"
  - level: PASS
    check: "No stale-slice-references (L-417)"
  - level: PASS
    check: "No GO-scope-not-propagated inception(s) (sibling to L-417)"
  - level: PASS
    check: "Fabric: 870 registered, 0 unregistered"
  - level: WARN
    check: "Fabric: 104/861 cards have no edges"
    mitigation: "Run: fw fabric enrich"
  - level: WARN
    check: "Fabric drift: 1 source file(s) have no fabric card"
    mitigation: "Run: fw fabric scan"
  - level: INFO
    check: "Cron drift checks skipped — linked worktree (cron is host-level, managed from the main checkout)"
  - level: INFO
    check: "Cron-misload lint skipped — linked worktree (cron install is host-level, managed from the main checkout)"
  - level: PASS
    check: "Hook threshold: no hooks failing over threshold"
  - level: PASS
    check: "Self-vendor drift: vendored .agentic-framework/ in sync with source (libs + templates)"
