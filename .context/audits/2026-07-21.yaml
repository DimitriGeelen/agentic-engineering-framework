# Audit Results - 2026-07-21-2355
timestamp: 2026-07-21T21:55:45Z
sections: "structure"
summary:
  pass: 23
  warn: 4
  fail: 0
findings:
  - level: PASS
    check: "Tasks directory exists"
  - level: PASS
    check: "Tasks/active directory exists"
  - level: PASS
    check: "Tasks/completed directory exists"
  - level: PASS
    check: "Tasks/templates directory exists"
  - level: PASS
    check: "Task template exists"
  - level: PASS
    check: "No duplicate task IDs across active/ and completed/"
  - level: PASS
    check: "All 25 project YAML files parse correctly"
  - level: PASS
    check: "All 13 arc anchor_task references resolve to existing tasks"
  - level: WARN
    check: "Arc 'arc-grooming' has no task commits in the last 30 days (39 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: WARN
    check: "Arc 'dispatch-safety' has no task commits in the last 30 days (11 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: WARN
    check: "Arc 'watchtower-redesign' has no task commits in the last 30 days (70 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: INFO
    check: "free driver F-AUTONOMY: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F3: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F1: retire_when text present, no recognition heuristic — manual review"
  - level: INFO
    check: "free driver F2: retire_when text present, no recognition heuristic — manual review"
  - level: PASS
    check: "No inline arc:<slug> tag-only scans outside canonical lib (T-1881)"
  - level: PASS
    check: "No stale-slice-references (L-417)"
  - level: PASS
    check: "No GO-scope-not-propagated inception(s) (sibling to L-417)"
  - level: PASS
    check: "Fabric: 897 registered, 0 unregistered"
  - level: WARN
    check: "Fabric: 14/811 cards have no edges"
    mitigation: "Run: fw fabric enrich"
  - level: PASS
    check: "Fabric drift: All watched source files registered (897 cards)"
  - level: PASS
    check: "Cron registry in sync with /etc/cron.d/agentic-audit-999-agentic-engineering-framework"
  - level: PASS
    check: "Hook threshold: no hooks failing over threshold"
  - level: PASS
    check: "Secret scan: tracked tree clean"
  - level: PASS
    check: "Large-file gate: tracked tree clean"
  - level: PASS
    check: "Self-vendor drift: vendored .agentic-framework/ in sync with source (libs + templates)"
  - level: PASS
    check: "Designer ghost registry: ghost↔task joins clean"
