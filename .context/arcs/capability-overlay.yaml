id: arc-010
slug: capability-overlay
name: "Capability overlay \u2014 framework MCP server + CLI route for agent-callable primitives"
description: "Path C-scoped (both-as-siblings) per T-2209 \xA710/\xA712. Four-slice sequence: (1)\
  \ read-only verbs + framework-mcp-baseline + OR-2 scan + scoping verbs; (2) framework\
  \ MCP server process + initial tool set; (3) agent-authority verbs (task_update,\
  \ work_on, note, context_focus); (4) Watchtower frontend migrates one blueprint\
  \ to JSON-shape. Sovereignty-bound verbs (bvp confirm, inception decide, arc close,\
  \ tier0 approve) stay shell-only \u2014 \xA73-foreclosed. Auth surface remains Candidate\
  \ D (env-inherit + binding conditions per IW-3)."
status: in-progress
anchor_task: T-2209
headline_mechanic: "Agent dispatches a task via mcp__fw__task_update / mcp__fw__work_on and works it\
  \ to work-completed; operator observes /review/T-XXX rendered correctly; transcript\
  \ JSONL shows no Bash(bin/fw ...) lines for those verbs"
demo_evidence: null
created: 2026-06-08T12:12:06Z
closed_at: null
decision: null
# T-1918 (arc-006, value-prioritisation): BVP scoring fields. Semantics in
# docs/reports/T-1915-bvp-inception.md §4 (D7-reframe) and §7 (M2).
bvp_scores: {}
scoped_drivers: []           # max 3, weight ≤6 each (M2). Approved by fw arc approve-driver (T-1926).
proposed_scoped_drivers: []  # uncapped persistence (D7-reframe). Estimator may write here freely.
