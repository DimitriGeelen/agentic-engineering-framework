# Working Memory - Current Focus
# Session: S-2026-0806-0011

current_task: T-2901
priorities: []
blockers: []
pending_decisions: []
reminders:
- Run audit before pushing
- Create handover before ending session
- "T-2893 IS THE PICKUP AND THE HARD PART IS ALREADY DONE. Three fixtures to author,\
  \ not two: (a) the clean case 832 asked for \u2014 our leading rationale opening\
  \ with their eight trailer words; (b) the incidental case \u2014 those words opening\
  \ a rationale that then runs on into genuinely different content, which is the shape\
  \ that actually occurred; (c) NEW, added at rail 497 \u2014 a document that names\
  \ NO producer and carries an authored leading rationale, because that is what all\
  \ 17 of our legacy carriers look like and 832's T-406 fix gates on naming a DIFFERENT\
  \ producer. If their answer to (c) is 'suppress', our documents lose their rationale\
  \ on import even after both fixes land. Hand over as REFS not bytes (OBS-108). Write\
  \ documents WE would plausibly author \u2014 the entire value of 832 asking us is\
  \ that the input comes from the party who would really produce it."
- "THE FINDING THAT CAME OUT OF T-2893 IS BIGGER THAN T-2893. We hold 832's T-406\
  \ defect. tools/corpus_spec.py:_is_boilerplate_comment (our T-2682) is `text.strip().startswith(_DI_TRAILER_PREFIX)`\
  \ \u2014 a pure text match, the exact mechanism they replaced with producer-identity\
  \ gating. Their reason transfers unchanged: when a peer's rationale is byte-identical\
  \ to the boilerplate, no string test separates them. Filed as T-2895. DO NOT just\
  \ port their fix: T-2682's docstring records the trailer was ALREADY laundered into\
  \ the doc slot on aef-audit-cron and aef-session-lifecycle, both already promoted\
  \ \u2014 so the text matcher is load-bearing against corruption that actually happened.\
  \ And identity gating covers only documents we GENERATE: the 17 legacy carriers\
  \ name no producer, and designer_api.py:141 writes client bytes verbatim by contract\
  \ (T-2530)."
- "T-2891 IS PARTIAL-COMPLETE AND THE HUMAN AC IS NOT MINE TO TICK. http://192.168.10.107:3001/review/T-2891\
  \ \u2014 the seam commitment (we now stamp exporter=\"aef-corpus-spec\" on emitted\
  \ <definitions>) is the operator's call because 832's own side is NOT ratified:\
  \ their frozen mapping standard is silent on exporter and they flagged it to their\
  \ operator the same day. Revert is `git revert 19d88cc9b` plus the two census assertions.\
  \ Recommendation filed GO with rationale."
- "FILED THIS SESSION, NOT FIXED \u2014 four tasks, each with the evidence already\
  \ measured: T-2889 (git stash classifies SAFE but writes the working tree \u2014\
  \ over-permission, the DANGEROUS direction, sibling to branch -d / tag -d); T-2892\
  \ (corpus all-versions census pinned at 32 while the live store is 37 \u2014 red\
  \ and unnoticed; answer BOTH halves, the pin AND why a red unit test survived sessions\
  \ unseen, do not just bump the number); T-2894 (port-3000 carriers have no set-level\
  \ ratchet \u2014 T-2732 gates the increment at task close only; raw counts 334 task\
  \ files / 19 active / 497 non-task, but DO NOT quote those as defects, they include\
  \ the sanctioned fallback and CLAUDE.md's own documentation of the anti-pattern\
  \ \u2014 classify carrier-vs-citation first); T-2895 (above)."
- "RAIL IS AT 497 AND 494/496/497 ARE MINE. 832's 495 crossed with my 494 in flight,\
  \ so they had not read the stamp answer when they wrote it. AWAITING FROM THEM:\
  \ (1) what their parser does with a document naming NO producer that carries an\
  \ authored leading rationale \u2014 asked at 497, and it decides whether our 17\
  \ legacy documents survive their import at all; (2) their operator's ruling on the\
  \ mapping standard, which is silent on exporter. NOT owed by them: anything on the\
  \ port ratchet (they said so explicitly)."
- "STILL OWED TO 832 \u2014 the one question deferred since 491 and STILL not answered:\
  \ does our learnings register have an application/applied field and what proportion\
  \ is set? Their census: 113 of 118 at `application: TBD`. Their reading is sharper\
  \ than 'unapplied backlog' \u2014 most learnings are behavioural and are applied\
  \ BY BEING READ, so 96% proves the FIELD CARRIES NO SIGNAL rather than that work\
  \ is pending. DO NOT QUOTE A NUMBER WITHOUT COUNTING IT. The revisit half is already\
  \ answered (revisit_at + revisit_evidence_needed, swept daily by agents/context/revisit-due-scan.sh\
  \ under G-053 \u2014 a dated commitment with a stated trigger, not a queue)."
- "TWO GATE FIXES SHIPPED AND VENDORED (T-2887, T-2888, T-2890). The echo/printf branch\
  \ had its own copy of the redirect regex that never got has_bash_write_pattern's\
  \ 2/& exemptions, so `echo x 2>&1` classified as a file write. And fourteen read-only\
  \ git verbs our own tooling uses were gated, including `git rev-list` \u2014 the\
  \ command the session-end rule prescribes, at the null-focus state work-completed\
  \ leaves behind. The load-bearing tests are the two that pin the CAUSE not the symptom:\
  \ the echo-vs-write-pattern agreement matrix, and 'no unclassified git verb is used\
  \ by our own tooling', which derives the verb set from our source at test time.\
  \ The latter found four verbs I had missed by eye \u2014 that failure was its positive\
  \ control."
- "I PUT `## Verification` IN THE WRONG PLACE TWICE THIS SESSION (T-2890, T-2891).\
  \ The template order is `### Agent` \u2192 `### Human` \u2192 `## Verification`.\
  \ Appending a Verification section straight after the Agent ACs puts the Human AC\
  \ prose BELOW it, and the extractor reads prose as shell commands \u2014 P-011 caught\
  \ it the second time by trying to execute 'and the agent will revert and post the\
  \ reversal to 832'. Put new body sections ABOVE `### Human`, and the Verification\
  \ block LAST."
- "L-518 IS EARNING ITS KEEP AND IS THE HIGHEST-VALUE HABIT ON THIS RAIL. Three times\
  \ this session a defect 832 reported in THEIR code was live in OURS: rail 489 \u2192\
  \ T-2887, rail 495 \u2192 T-2894, T-406 \u2192 T-2895. In every case I found it\
  \ by measuring our equivalent rather than by reading their report and agreeing.\
  \ Twice the measurement contradicted my first hypothesis \u2014 the 2>/dev/null\
  \ guess in T-2887 was simply wrong, disproved by a command that ran fine."
- T-2720 IS THE OPEN ARC-017 KEYSTONE AND IT STAYS OPEN. Two unticked [REVIEW] ACs,
  both Steps blocks verified before handoff. NEVER TICK THESE. Hand over with `bin/fw
  task review T-2720` and QUOTE THE EMITTED URL VERBATIM.
- "ARC-016 IS CLOSURE-BLOCKED ONLY ON A HUMAN RUN NOW. Keystone T-2719 recorded 'fix\
  \ OBS-170, then run the by-hand path once more and capture that run as the arc's\
  \ --demo'. OBS-170 is done (T-2883). The remaining clause is a live by-hand walk\
  \ \u2014 OPERATOR'S, not agent's (\xA7ACD / G-062, and `fw arc close` refuses under\
  \ CLAUDECODE anyway). Do NOT close it. Surface via the Watchtower URL, never the\
  \ CLI (T-2347)."
- "STILL FILED, NOT FIXED: OBS-201 (7MB `yaml,sys` capture in repo root \u2014 DO\
  \ NOT DELETE, it is evidence, and `git add -A` trips the secret scanner on it; stage\
  \ selectively). OBS-202 through OBS-207. G-078 NOT CLOSED \u2014 and it bit again\
  \ at this session's start: the task gate blocked `git rev-list` and `termlink`,\
  \ which is exactly the class."
- "DESIGNER STAYS PINNED AT 0.8.0. 0.9.0 is fetchable (public mirror github.com/DimitriGeelen/workflow-designer,\
  \ tag designer-v0.9.0 at 8cd0c5d3). Repin needs its OWN TASK and operator sign-off\
  \ \u2014 the designer is served live in Watchtower. T-559 unchanged: NEVER read\
  \ 832's working tree."
focus_session: S-2026-0806-0011
