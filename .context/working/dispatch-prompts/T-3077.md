You are working in /opt/999-Agentic-Engineering-Framework on task **T-3077**.

Run `bin/fw work-on T-3077` first, then read
`.tasks/active/T-3077-governance-test-suite-leaves-live-tier-0.md` **in full**.
Its `## Context` is the evidence base and its six ACs are the deliverable. Do not
re-derive the RCA; it is already measured.

# Scope — exactly one deliverable

Stop `tests/governance/test_pretooluse_gates.bats` writing real Tier 0 approval
requests into the live project, and pin that it stays stopped.

An operator opened Watchtower `/approvals` today and found live PENDING cards for
`rm -rf /` and `git push --force origin master`, each with an Approve button.
Approving writes the command hash into `.context/working/.tier0-approval`, which
pre-authorises that exact command for the TTL. Nothing ran and nothing was ever
granted — but the queue was handing the operator a one-click authorisation for the
most destructive command there is, generated by a test.

# The fix is already prescribed — apply it, don't redesign it

**L-256** (T-1428, 2026-04-24) names this exact bug and its fix:

> bats tests that invoke hooks writing to `.context/` MUST export
> `PROJECT_ROOT=TEST_TEMP_DIR` in `setup()`.

Verified still applicable: `lib/paths.sh:39` guards derivation with
`if [[ -z "${PROJECT_ROOT:-}" ]]`, so an env override is honoured.
`check-tier0.sh:63` derives `APPROVAL_FILE` from `PROJECT_ROOT`, and
`check-tier0.sh:466` derives `APPROVAL_DIR` from it too (with its own
`${APPROVAL_DIR:-...}` override). The suite currently has **no `setup()` at all**.

Delete `_tier0_isolate` / `_tier0_restore` rather than extending them. They are the
defect: they back up `.tier0-approval` (the GRANTED file) and neither of the two
files the hook actually writes. A helper that names the wrong file looks identical
to one that works — that is why this survived four months.

# Watch for this while you work

Every hook in that suite is invoked as `"$HOOK_BIN" hook <name>` in a `bash -c`
subshell. Exporting `PROJECT_ROOT` in `setup()` must actually reach that subshell.
Verify it does; do not assume. If a hook needs more of a project skeleton than an
empty temp dir provides (`.context/working/`, `.tasks/`), create it in `setup()` —
but check first whether the hook already tolerates its absence, and say which.

**Two tests in this suite currently fail and are NOT yours to fix:**
`governance #5 (check-active-task: blocks Write to source when no active task)` and,
in the sibling integration suite, `#16 (Bash redirect to /etc)`. Take a baseline
before your change and report before/after counts. If your change alters either
one — including turning #5 green — say so explicitly; that is information, not a
side quest, and it may indicate your isolation changed more than intended.

# Acceptance criteria — all six (A1-A6, read them in the task file)

A1 asserts **content**, not counts: snapshot `.context/approvals/` and
`.context/working/.tier0-approval*` (a manifest of paths + sha256), run the full
suite, compare. A card created and then deleted must still fail A1 — it was visible
on `/approvals` for the duration of the run.

A3 is the positive control and it is the AC most likely to be faked: assert a
pending card **is** written inside the sandbox — proving the hook still fired, still
blocked, and still filed its request — and is **not** written to the live tree.
Without the first half, A1 passes for a test that has quietly stopped testing.

A5: real mutation. Revert to the old `_tier0_isolate`/`_tier0_restore`, record which
tests flip red **by name**, restore, confirm green. "I mutation-tested it" without a
list is not evidence.

A6: update **L-256 in place** with `closed_by` and state precisely what it now
covers. L-256 also names `observe` and `handover` (and cites L-227, L-009) — if
those are still unfixed, L-256 is only partly closed and must say so. Do NOT file a
second learning. A4 asks you to survey which other test files invoke `.context/`-
writing hooks against the live tree and RECORD the finding in the task; fix only
this suite (one bug, one task) and file the rest as a follow-up task if warranted.

# Hard constraints

1. **Leave the live approvals surface clean when you finish.** You will run the
   suite, and until your fix lands each run files real cards. Before your final bus
   post, run `bin/fw tier0 status` and confirm it prints `Clean: No pending blocks
   or approvals`; if not, remove `.context/approvals/pending-*.yaml` and
   `.context/working/.tier0-approval.pending` (named paths only, never a glob'd
   recursive delete) and re-check. State the final status in your bus post.
2. **Never create `.context/working/.tier0-approval`** (the granted file) or any
   `.context/approvals/resolved-*.yaml`. Those grant real authority. Tests must
   assert on the sandbox copies only.
3. Do not weaken any gate to make a test pass. If a test fails because a gate is
   doing its job, that is the correct outcome — report it.
4. Assertions scoped to the state under test: exit codes and specific lines, never
   a bare `*"BLOCKED"*` substring match on multi-line stderr.
5. New test file in `tests/governance/` or `tests/lint/`, named for T-3077, and
   **`git add` it** — `tests/lint/no-untracked-test-files.bats` will fail otherwise
   (T-3074).
6. Do not edit `CLAUDE.md`, `.claude/settings.json`, or any permission file.
7. Do not edit `agents/context/check-tier0.sh` unless you can show the fix cannot
   be made test-side; if you touch it, justify it in the bus post. The hook writing
   a pending record when it blocks is correct behaviour.
8. Commits reference `T-3077:`. Repo paths only, never `/tmp` for outputs (T-818).
9. Tick an Agent AC the moment its work lands. Never tick a `### Human` AC.

# When you are done

    bin/fw bus post --task T-3077 --agent build \
      --summary "<one line>" --result "<per-file changes; before/after test counts; mutation result with test names; A4 survey; final 'fw tier0 status'; anything unverified>"

Do NOT set the task to work-completed. Final reply under 300 words.
