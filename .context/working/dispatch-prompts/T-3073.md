You are working in /opt/999-Agentic-Engineering-Framework on task **T-3073**.
Run `cd /opt/999-Agentic-Engineering-Framework && bin/fw work-on T-3073` first, then
read `.tasks/active/T-3073-c-001-research-artefact-check-is-blind-t.md` in full —
including its `## Decisions`, which already settles where the predicate lives. Do not
re-litigate that decision.

# Scope — exactly one deliverable

Widen the C-001 research-artefact audit check so it covers inceptions that are being
**decided**, not only inceptions that are being **worked**.

Today `agents/audit/active-task-scan.py:170` reads:

```python
if workflow_type == "inception" and status == "started-work":
```

Measured 2026-08-18: the scan sees 4 started-work inceptions and reports 1 missing
artefact. Meanwhile 6 inceptions sit in `fw review-queue` asking the operator for a
go/no-go, **none of them has a `docs/reports/T-XXX-*.md` artefact**, and five are
`status: captured` — invisible to the rail built to catch exactly this.

# The design, already decided (task `## Decisions`)

- Cheap Python pre-filter inside the single-pass scan narrows to inceptions that
  *look* like they carry a `## Recommendation`.
- Authoritative confirmation shells out to the existing bash predicate
  `audit_inception_recommendation` (`lib/task-audit.sh:117`) **once per candidate**,
  not once per task. That function is the definition used by `fw inception decide`,
  `fw task review` and `fw task review-batch`, and carries three regression fixes
  (T-1528, T-1510, T-1746). Do not re-express it in Python — a second definition of
  the same rule is what T-3061 was opened to remove.
- `active-task-scan.py` exists to read each of ~130 task files exactly once (T-955).
  Do not add a subprocess per task.

# Acceptance criteria (from the task — implement all five)

- **A1** the check covers recommendation-bearing inceptions regardless of `status:`
- **A2** an inception with neither a recommendation nor started-work stays silent
- **A3** positive control: the test asserts a known-missing case IS reported *and* a
  known-satisfied case is NOT (L-616 — two empty sets are equal, so a passing test
  over an empty set proves nothing)
- **A4** `fw audit` distinguishes the two populations in what it reports, so widening
  the set does not silently inflate a number the operator reads one way today
- **A5** mutation-tested: revert the set-widening → new test goes red; restore → green

# Hard constraints

1. **WARN, never FAIL.** This is hygiene about work already done; it must not block a
   push. Match how the T-3061 `unclosed_satisfied` rail reports (`agents/audit/audit.sh`).
2. **Assertions must be scoped to the state under test.** `[[ "$output" == *"WARN"*"my
   message"* ]]` passes against any verdict, because a ~40-line doctor/audit run has
   almost always already printed a WARN somewhere. That exact weakness was found by
   mutation testing on T-3064 this session. Scope to the line.
3. Do **not** retro-write artefacts for the six tasks the rail will surface — the rail
   is the deliverable; what the operator does with the six is their call.
4. Do not edit `bin/fw`, `lib/upgrade.sh`, `lib/init.sh` or `agents/designer/` —
   another worker holds those for T-3064.
5. Do not edit `CLAUDE.md` or any permission/settings file.
6. Commits reference `T-3073:`. Write to repo paths only, never `/tmp` (T-818).
7. Tick an Agent AC only when its work is actually in place, at the moment it lands.
   Never tick a `### Human` AC.

# When you are done

    bin/fw bus post --task T-3073 --agent build \
      --summary "<one line>" --result "<per-file changes; ACs open; anything unverified>"

Do NOT set the task to work-completed. Final reply under 300 words — the orchestrator
has limited context budget.
