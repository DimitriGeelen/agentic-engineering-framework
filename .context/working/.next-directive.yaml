# Continuous-run directive — updated 2026-06-13 after T-2364+T-2365 shipped.
# Next session reads this via:
#   1. agents/context/inject-next-directive.py (T-2364 substrate, when enabled in
#      .context/working/.continuous-mode.yaml — currently enabled: true)
#   2. agents/context/checkpoint.sh fold-into-restart-signal (T-2363) on next auto-handover
#   3. claude-fw FW_NEXT_DIRECTIVE export (T-2363) on next auto-restart
# Iteration counter at .context/working/.continuous-mode.yaml: current_iteration=1.

directive: |
  Continue arc-012 continuous-run. T-2362 (S0), T-2363 (S1), T-2364 (S2),
  T-2365 (S3) all SHIPPED. Remaining slices:
    - T-2367 (S5): post-resume Tier-0 + blast-radius re-check before
      continuing. This is the bounded-autonomy CEILING — without it the
      tier_ceiling field is informational only. ACs at .tasks/active/T-2367-*.
      Helper at agents/context/inject-next-directive.py needs:
        - Parse the directive text for a T-NNNN reference (the "planned
          next action").
        - Look up that task's blast_radius (cost_estimate.blast_radius or
          fw fabric blast-radius shortcut).
        - When blast_radius > tier_ceiling (from .continuous-mode.yaml),
          replace the directive section with "Tier ceiling exceeded —
          operator continuation required" and freeze the iteration counter.
    - T-2366 (S4): discard manifest enhancement to fw handover --emergency
      (lower priority; horizon: later).
  Continuous-mode is enabled with max_iterations=10, current_iteration=1.
  Per-directive max_iterations=5 in THIS file overrides the config cap when
  set. The substrate has been live-fired twice in this session
  (source=resume and source=compact) — see iteration 1/5 and last_source
  fields in .continuous-mode.yaml. After S5 ships, T-2158's GO scope is
  effectively closed (S0-S3 + S5 = minimum-viable continuous loop with
  safety ceiling; S4 is a horizon: later enhancement).

filed_by: agent-T-2365-handover
filed_at: 2026-06-13T09:46:00Z

# Continuous-mode bounding (overrides .continuous-mode.yaml when set):
expires_at: 2026-06-15T00:00:00Z
max_iterations: 5
tier_ceiling: 1
