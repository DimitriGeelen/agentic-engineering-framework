# Volatile session files — regenerated each session
.tool-counter
.prev-token-reading
session.yaml
focus.yaml
tier0-approval

# Runtime activity logs and per-machine state (T-1435)
.publish-learning-bus.log
pending-updates.yaml
