# ============================================================================
# EPISODIC MEMORY - T-2289: lib/paths.sh — re-derive TASKS_DIR/CONTEXT_DIR when PROJECT_ROOT is explicitly
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-09T15:06:22Z
# ============================================================================

task_id: T-2289
task_name: "lib/paths.sh — re-derive TASKS_DIR/CONTEXT_DIR when PROJECT_ROOT is explicitly"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-09T14:15:57Z
completed: 2026-06-09T15:06:16Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - '`lib/paths.sh` exports `_FW_PATHS_DERIVED_BY="$PROJECT_ROOT"` after derivation'
  - '`lib/paths.sh` unsets `TASKS_DIR`/`CONTEXT_DIR` when `_FW_PATHS_DERIVED_BY` is set AND differs from current `PROJECT_ROOT`'
  - 'Env-leak reproducer: outer sources with `PROJECT_ROOT=/tmp/proj-A`; subshell with `PROJECT_ROOT=/tmp/proj-B` re-sources → `TASKS_DIR=/tmp/proj-B/.tasks`'
  - 'Fixture invariant preserved: same-shell explicit `TASKS_DIR` override (no prior derivation, sentinel absent) survives intact'
  - 'Fresh-invocation invariant: derives `TASKS_DIR=$PROJECT_ROOT/.tasks` + `CONTEXT_DIR=$PROJECT_ROOT/.context` exactly as before'
  - 'New bats `tests/unit/t2289_paths_env_leak.bats` covers all 5 scenarios — 5/5 PASS'
  - '`fw reviewer T-2289` returns Overall PASS (3 AC-verify-mismatch FPs suppressed via OV-3fd9517e/OV-26f42554/OV-1d8cb020 — same class as T-2284/T-2285/T-2288)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 50 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [paths-sh, env-leak, framework-bug, class-signal, obs-053]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 50
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/999-Agentic-Engineering-Framework/.tasks/completed/T-2289-libpathssh--re-derive-tasksdircontextdir.md
generated_by: context-agent-hybrid
