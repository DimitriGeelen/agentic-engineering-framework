# ============================================================================
# EPISODIC MEMORY - T-593: Build fw-util
# ============================================================================
# STATUS: ENRICHED
# Built the TypeScript multi-subcommand utility that replaces ~290 inline python3 -c blocks.

task_id: T-593
task_name: "Build fw-util — TypeScript multi-subcommand utility replacing inline Python"
completed: "2026-03-24T06:32:37Z"
workflow_type: build

summary: |
  Built fw-util TypeScript binary with 8 subcommands replacing ~290 inline python3 -c blocks across 49 scripts.

what_happened: |
  Implemented fw-util as a single TypeScript binary (93KB esbuild bundle) with 8 subcommands:
  yaml-get, yaml-set, json-get, json-set, path-rel, path-resolve, date-fmt, and frontmatter.
  These replace ~290 inline python3 -c blocks across 49 bash scripts, eliminating shell escaping
  vulnerabilities identified in T-586. All subcommands read data from files/args (never shell
  interpolation), achieving <30ms execution time per invocation.

key_decisions: []

patterns_observed:
  - "Single-binary multi-subcommand pattern (like busybox) keeps the interface simple for bash callers while consolidating all data-manipulation logic in one place."
  - "Stdin/argv-based interface eliminates shell escaping vulnerabilities that plagued inline python3 -c blocks."

artifacts:
  - "lib/ts/src/fw-util.ts"
  - "lib/ts/dist/fw-util.js"
  - "lib/ts/package.json"
  - "lib/ts/package-lock.json"

lessons: []

reuse_value: "high — fw-util is foundational infrastructure; all future bash-to-TS migration (T-595) depends on this binary and its subcommand dispatch pattern"
