# ============================================================================
# EPISODIC MEMORY - T-521: fw init should git init when not in a git repo
# ============================================================================
# STATUS: ENRICHED
# Added automatic git init + initial commit during fw init when no git repo exists.

task_id: T-521
task_name: "fw init should git init when not in a git repo"
completed: "2026-03-17T22:45:38Z"
workflow_type: build

summary: |
  Added git init to fw init flow — new projects now get a git repo automatically since traceability requires it.

what_happened: |
  fw init assumed it was running inside a git repo, but new projects don't have one.
  Git hooks, traceability (P-002), and commit-msg enforcement all require git. Added
  automatic git init + initial commit to the fw init flow when no .git directory exists.

key_decisions: []

patterns_observed:
  - "Framework setup must bootstrap its own prerequisites — can't assume git exists"

artifacts:
  - "lib/init.sh"

tags: [enhancement, install, D3]

metrics:
  wall_clock_minutes: 6
  commits: 1
