# ============================================================================
# EPISODIC MEMORY - T-452: PR #5: T-003: Auto-create onboarding tasks
# ============================================================================
# STATUS: ENRICHED
# Tracked OneDev PR #5 which replaces the static onboarding menu with auto-detected tasks.

task_id: T-452
task_name: "PR #5: T-003: Auto-create onboarding tasks after fw init"
completed: "2026-03-12T13:00:47Z"
workflow_type: build

summary: |
  PR #5 merged — replaced static onboarding menu with auto-detection of existing vs new projects, creating appropriate tasks automatically.

what_happened: |
  Reviewed and merged OneDev PR #5 (branch: fix/T-003-auto-onboarding-tasks).
  The PR replaced the static "Choose your path" menu in fw init with auto-detection:
  existing projects (detected via package.json, requirements.txt, src/, etc.) get 3
  onboarding tasks (ingest, fabric register, handover), while new projects get an
  inception task for goals/architecture. The implementation is idempotent — safe
  for --force re-init. Changed 69 lines added / 16 removed in lib/init.sh.

key_decisions: []

patterns_observed:
  - "Auto-detection of project type (existing vs new) is more user-friendly than interactive menus for CLI onboarding"
  - "Idempotency on re-init prevents duplicate task creation"

artifacts:
  - "lib/init.sh"

lessons: []

reuse_value: "medium — the auto-detection onboarding pattern (existing project vs greenfield) is reusable for any CLI tool with diverse entry points"
