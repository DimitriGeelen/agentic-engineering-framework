# ============================================================================
# EPISODIC MEMORY - T-1627: B-1 (T-1626): CWD-invariant hook resolution — fix .agentic-framework/bin/fw not-found from subdirs
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-04-30T21:27:48Z
# ============================================================================

task_id: T-1627
task_name: "B-1 (T-1626): CWD-invariant hook resolution — fix .agentic-framework/bin/fw not-found from subdirs"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-04-30T21:19:21Z
completed: 2026-04-30T21:27:44Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - "`tests/unit/upgrade_relative_hook_path_detection.bats` exists with ≥6 cases pinning the fix shape (source markers + behavioural detector tests)"
  - "All 6 bats cases pass: source contains `T-1627`, `check_stale_paths` references `.agentic-framework` prefix shape, bash -n parses, detector flags bare-relative path, detector PASSES absolute path, detector PASSES `$CLAUDE_PROJECT_DIR`-prefixed path, detector still flags legacy `/agents/context/` (backwards compat)"
  - "`lib/upgrade.sh` `check_stale_paths` has a new branch detecting bare-relative `.agentic-framework/bin/fw` (path starts with `.agentic-framework/` after lstrip)"
  - "`bash -n lib/upgrade.sh` parses clean"
  - "Synthetic settings.json with bare-relative path triggers `needs_regen=true` on `fw upgrade --dry-run`"
  - "Synthetic settings.json with absolute path does NOT trigger regen on `fw upgrade --dry-run` (false-positive guard)"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 8 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [hooks, resilience, from-T-1626, B-1]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 8
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/999-Agentic-Engineering-Framework/.tasks/completed/T-1627-b-1-t-1626-cwd-invariant-hook-resolution.md
generated_by: context-agent-hybrid
