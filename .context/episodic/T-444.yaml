# ============================================================================
# EPISODIC MEMORY - T-444: PR #3: L-077/L-078/L-079: Pre-compact cleanup
# ============================================================================
# STATUS: ENRICHED
# Tracked OneDev PR #3 which codified three learnings and completed the hook set in fw init.

task_id: T-444
task_name: "PR #3: L-077/L-078/L-079: Pre-compact cleanup learnings + complete hook set in fw init"
completed: "2026-03-12T13:00:47Z"
workflow_type: build

summary: |
  PR #3 merged — codified L-077/L-078/L-079 learnings, completed hook set in fw init, fixed PROJECT_ROOT resolution across 19 agent scripts.

what_happened: |
  Reviewed and merged OneDev PR #3 (branch: learning/precompact-cleanup). The PR
  codified three learnings: L-077 (PROJECT_ROOT must resolve from git toplevel, not
  hardcoded), L-078 (delete redundant project artifacts when framework catches up),
  and L-079 (token monitoring needs both structural enforcement via hook AND behavioral
  guidance via skill). It also completed the hook set generated by fw init: PreCompact
  hook, SessionStart hooks, budget-gate hook, token-monitor skill, and a manual
  check-tokens command. A companion commit (71940104) fixed PROJECT_ROOT resolution
  across all 19 agent scripts. Total: ~170 lines added across learnings.yaml,
  budget-gate.sh, and lib/init.sh.

key_decisions: []

patterns_observed:
  - "Learnings that affect multiple scripts (like PROJECT_ROOT resolution) benefit from a single sweep commit across all affected files"
  - "Token monitoring requires dual enforcement: structural (hook blocks) plus behavioral (skill guidance)"

artifacts:
  - ".context/project/learnings.yaml"
  - "agents/context/budget-gate.sh"
  - "lib/init.sh"
  - "agents/context/check-tier0.sh"
  - "agents/context/checkpoint.sh"
  - "agents/context/context.sh"
  - "agents/context/post-compact-resume.sh"
  - "agents/context/pre-compact.sh"
  - "agents/fabric/fabric.sh"
  - "agents/git/git.sh"
  - "agents/handover/handover.sh"
  - "agents/healing/healing.sh"
  - "agents/resume/resume.sh"
  - "agents/task-create/create-task.sh"
  - "agents/task-create/update-task.sh"

lessons:
  - "L-077: PROJECT_ROOT must resolve from git toplevel, not from hardcoded paths"
  - "L-078: Delete redundant project artifacts when framework catches up"
  - "L-079: Token monitoring needs both structural enforcement (hook) AND behavioral guidance (skill)"

reuse_value: "high — the dual-enforcement pattern (structural hook + behavioral skill) is a reusable governance principle, and the PROJECT_ROOT resolution fix is a cross-cutting concern template"
