# ============================================================================
# EPISODIC MEMORY - T-3248: Nothing measures useful-headroom (WINDOW minus BASELINE) - the ratio that decides whether an armed loop progresses or spins
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-07T07:11:37Z
# ============================================================================

task_id: T-3248
task_name: "Nothing measures useful-headroom (WINDOW minus BASELINE) - the ratio that decides whether an armed loop progresses or spins"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-09-01T20:24:30Z
completed: 2026-09-07T07:11:17Z
duration_days: 6
updates_count: 2
first_commit: 2026-09-01T22:25:32+02:00
last_commit: 2026-09-07T09:03:18+02:00

# Summary (auto-generated from git commit messages)
summary: |
  file the useful-headroom gap found by arc-012 E9. measure useful-headroom (WINDOW - BASELINE) — the ratio E9 showed nothing was watching. register fabric card for useful-headroom test suite

# Key outcomes
outcomes:
  - '`BASELINE` is derived from a real session''s own transcript, not a constant — a session with a heavier CLAUDE.md or hook set reports a higher baseline than a lighter one, demonstrated with two measurements that differ. (Fixtures: heavy=52600 vs light=12000 in `tests/unit/t3248_useful_headroom.bats`; live cross-check: parent session baselined 74,967, this worker session 85,964 — same host, different injected context, different floor.)'
  - 'The useful-headroom figure (`WINDOW - BASELINE`, and the ratio to `WINDOW`) is readable from at least one surface an operator or agent already looks at, without running a bespoke script. (`checkpoint.sh status` — the existing "where am I" surface — now prints `Useful headroom: N tokens (cap W - baseline B ...; ratio R)`; `checkpoint.sh budget` passes the fields through.)'
  - 'The figure is present in `.context/working/.budget-status` alongside `level` and `tokens`, so it is machine-readable by the loop itself and not only by a human reading prose. (budget-gate.sh rides `baseline_tokens`/`headroom_tokens`/`headroom_ratio` along in every cache write; nulls — never fabricated numbers — on the unknown branches.)'
  - 'A negative control exists: a deliberately constrained configuration (small window) reports a visibly poor ratio, and the default configuration reports a healthy one — so the measurement is shown to move, not merely to render. (Pinned hermetically: `FW_CONTEXT_WINDOW=58000` — the E9 dial — against the 52.6k-floor fixture yields ratio 0.09; default 300000 yields 0.82.)'
  - 'No threshold, gate, or WARN ships in this task. If one is warranted it is filed separately, with the observed distribution as its evidence. (Pinned by the `no gate ships` test: the poor-ratio run still exits 0 and emits no headroom-based stderr; no threshold constant added anywhere.)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '2026-09-07 — BASELINE derivation rule'
    chose: 'BASELINE = the total (`input_tokens + cache_read_input_tokens + cache_creation_input_tokens`) of the EARLIEST usage entry of the dominant model since the last `compact_boundary`, filtered by `.session-start-ts` — i.e. the context the session already occupied at its first measured API call: system prompt, CLAUDE.md, hook set, injected handover, opening prompt. Same scoping rules as `lib/context_tokens.py` (which takes the LAST in-scope entry = current usage; baseline takes the FIRST = the floor), including the same ≥2-in-scope-entries trust guard (0 = "not yet measurable", never a guess). Cached per `(transcript, session-start-ts)` in `.context/working/.session-baseline`; the cache drops on `checkpoint.sh reset` and on detected compaction (the floor is re-paid then).'
    rationale: 'Per-session-measured by construction — a heavier CLAUDE.md or hook set inflates the first turn''s snapshot and thus the baseline (verified live: parent session 74,967 vs this worker 85,964 on the same host). No new instrumentation: it is the same transcript JSONL the gauges already read. The full-file scan (no `tail -c`, unlike the current-usage read) is required because the first entry lives at the HEAD of the post-boundary region, which a tail window truncates on long transcripts — and it runs once per session thanks to the cache.'
    alternatives_rejected: ['(a) A constant (e.g. the E9 52.6k) — defeats the AC and goes stale the moment CLAUDE.md changes. (b) Sharing code by extending `lib/context_tokens.py` — the lib is outside this task''s write-set; the scan is inlined in `checkpoint.sh` with an explicit keep-in-step comment instead (accepted duplication, flagged for a future refactor if the lib''s scoping rules ever move). (c) Writing headroom fields from `checkpoint.sh` post-tool — budget-gate.sh is the sole writer of `.budget-status`, and a second writer would make the fields flap; instead budget-gate calls `checkpoint.sh baseline` (cache-served after first compute) and rides the fields along in its own write.']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.agentic-framework/agents/context/budget-gate.sh'
  - '.agentic-framework/agents/context/checkpoint.sh'
  - 'agents/context/budget-gate.sh'
  - 'agents/context/checkpoint.sh'
  - '.context/monitors/watchtower-rss.jsonl'
  - '.context/monitors/watchtower-rss-latest.yaml'
  - '.context/working/.hook-counter'
  - '.fabric/components/tests-unit-t3248_useful_headroom.yaml'
  - '.tasks/active/T-3248-nothing-measures-useful-headroom-window-.md'
  - 'tests/unit/t2885_context_tokens_model_scope.bats'
  - 'tests/unit/t3248_useful_headroom.bats'

# Timeline (auto-mined from git log)
git_timeline:
  - time: '2026-09-01T22:25:32'
    action: 'T-3248: file the useful-headroom gap found by arc-012 E9'
  - time: '2026-09-07T09:02:47'
    action: 'T-3248: measure useful-headroom (WINDOW - BASELINE) — the ratio E9 showed nothing was watching'
  - time: '2026-09-07T09:03:18'
    action: 'T-3248: register fabric card for useful-headroom test suite'

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 3 commit(s), 7846 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 7846
  commits: 4
  files_changed: 31
  lines_added: 1333
  lines_removed: 112
  footprint_refreshed_at: '2026-09-07T07:12:19Z'
  footprint_refreshed_by_commit: 13f84698d

# Metadata
source_file: /opt/999-Agentic-Engineering-Framework/.tasks/completed/T-3248-nothing-measures-useful-headroom-window-.md
generated_by: context-agent-hybrid
