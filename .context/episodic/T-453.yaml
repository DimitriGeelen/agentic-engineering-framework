# ============================================================================
# EPISODIC MEMORY - T-453: PR #4: T-002: Fix governance activation gap
# ============================================================================
# STATUS: ENRICHED
# Tracked OneDev PR #4 which fixes governance not being active after fw init.

task_id: T-453
task_name: "PR #4: T-002: Fix governance activation gap in fw init"
completed: "2026-03-12T13:00:47Z"
workflow_type: build

summary: |
  PR #4 merged — added fw context init to do_init() for immediate governance activation after fw init, hardened check-active-task.sh.

what_happened: |
  Reviewed and merged OneDev PR #4 (branch: fix/T-002-governance-activation-gap).
  The PR added a call to fw context init at the end of do_init() so that governance
  (focus.yaml, session.yaml) is active immediately after fw init. It also hardened
  check-active-task.sh to block when .framework.yaml exists but focus.yaml is missing,
  distinguishing initialized projects from true bootstrap. An integration test was added.

key_decisions: []

patterns_observed:
  - "PR-tracking tasks created from OneDev have minimal local commit history since work happened on feature branches"

artifacts:
  - "agents/context/check-active-task.sh"
  - "lib/init.sh"
  - "tests/integration/check_active_task.bats"

lessons: []

reuse_value: "medium — demonstrates the pattern of closing governance gaps where init leaves the project in a partially-governed state"
