# ============================================================================
# EPISODIC MEMORY - T-2166: value-drivers.yaml v3 schema rename: schema_version→version, D1-D4 rationale→note,
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-01T17:50:20Z
# ============================================================================

task_id: T-2166
task_name: "value-drivers.yaml v3 schema rename: schema_version→version, D1-D4 rationale→note,"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-01T17:43:03Z
completed: 2026-06-01T17:50:10Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - '`policy/value-drivers.yaml` `schema_version: 1` is replaced with `version: 3` (top-level field rename + bump).'
  - 'Each of D1-D4 in `protected_drivers:` carries exactly: `id`, `name`, `weight`, `note:` (renamed from `rationale:`). Existing `weight:` values preserved (9/7/5/3). New `note:` text restates the existing `rationale:` content (CLAUDE.md directives unchanged in meaning). Per the verbatim proposal, `protected: true` is REMOVED from D1-D4 — protection now lives in list-membership semantics (`protected_drivers:` section name) and the lib/bvp.sh:837 ID-prefix pattern, both confirmed safe by the T-2165 walk. Rubric for D1-D4 stays in `policy/bvp-scoring-rubric.md` (T-1921) — D1-D4 do NOT carry `rubric:`/`guardrails:`/`polarity:` fields.'
  - '`free_drivers:` is no longer `[]` — it has **two active entries** F-RECALL (weight 6) and F-ORCH (weight 5) plus **one commented carve** F-AUTONOMY. Each active entry carries: `id`, `name`, `weight`, `rationale:`, `polarity: positive`, `rubric:` (mapping with keys 0..5), `guardrails:` (multi-line string), `retire_when:` (free-text string). F-AUTONOMY is fully commented out as a YAML carve (not parsed; preserves the proposal''s "candidate not active" framing).'
  - '`auto_promote:` block unchanged: `enabled: false`, `bvp_norm_min: 0.85`, `cost_max: 1`, `max_concurrent: 1`. (Activation is §ACD-gated separately; this task touches schema only.)'
  - 'File header docstring (lines 1-39) updated to mention v3 and the F-RECALL/F-ORCH free drivers; references to `schema_version` updated to `version`; reference to T-2157/T-2165 added under filing.'
  - '`python3 -c "import yaml; d=yaml.safe_load(open(''policy/value-drivers.yaml'')); assert d[''version'']==3; assert len(d[''protected_drivers''])==4; assert len(d[''free_drivers''])==2; print(''shape ok'')"` — passes (single-line invariant check).'
  - 'Smoke test: `bin/fw bvp` (rank command, no args) exits 0 and produces output — confirms no reader broke on the schema change.'
  - 'No other source files touched: `git diff --name-only -- '':!.tasks'' '':!.context''` shows only `policy/value-drivers.yaml`.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 7 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [arc:value-prioritisation, value-drivers, bvp, schema]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 7
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/999-Agentic-Engineering-Framework/.tasks/completed/T-2166-value-driversyaml-v3-schema-rename-schem.md
generated_by: context-agent-hybrid
