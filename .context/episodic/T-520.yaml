# ============================================================================
# EPISODIC MEMORY - T-520: Fix commit-msg hook FRAMEWORK_ROOT resolution for vendored installs
# ============================================================================
# STATUS: ENRICHED
# Fixed hook path resolution after T-498 removed framework_path field from .framework.yaml.

task_id: T-520
task_name: "Fix commit-msg hook FRAMEWORK_ROOT resolution for vendored installs"
completed: "2026-03-17T22:45:36Z"
workflow_type: build

summary: |
  Fixed commit-msg hook — added .agentic-framework/ vendor path fallback after T-498 removed framework_path config field.

what_happened: |
  The commit-msg hook resolved FRAMEWORK_ROOT from .framework.yaml's framework_path field,
  but T-498 removed that field. The hook fell back to PROJECT_ROOT where lib/tasks.sh doesn't
  exist (it's in .agentic-framework/lib/). Fixed by adding vendor directory fallback resolution.

key_decisions: []

patterns_observed:
  - "Removing a config field (T-498) broke a downstream consumer (commit-msg hook) — blast radius not checked"
  - "Vendored install paths differ from in-repo paths, requiring separate resolution logic"

artifacts:
  - "agents/git/lib/hooks.sh"

tags: [bug, hooks, install, D2]

metrics:
  wall_clock_minutes: 6
  commits: 1
