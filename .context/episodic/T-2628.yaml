# ============================================================================
# EPISODIC MEMORY - T-2628: Corpus squash burst RCA — operator delete-all+resave regressed task-lifecycle
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-27T18:01:28Z
# ============================================================================

task_id: T-2628
task_name: "Corpus squash burst RCA — operator delete-all+resave regressed task-lifecycle"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-27T17:56:52Z
completed: 2026-07-27T18:01:16Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - 'Full damage assessment recorded: per map — content era of the re-saved v1 (which pre-burst version it matches), uuid presence in workflowMeta and meta.json, ghost-registry impact of any uuid loss — see §Damage assessment table: zero content loss, every v1 = latest pre-burst content'
  - 'RCA section filled: what fired the 13-delete burst (operator flow vs editor bug), why the content looked regressed, why uuid looked dropped — both initial reads disproven with evidence'
  - 'Restore path decided: NO restore needed — zero content loss proven canonically; squash committed to git as the operator''s intended store state (not silently reverted); finding reported to operator in session summary'
  - 'Conformance rail + corpus lint state after resolution: rail alive (same DIVERGENT 2-pair signal, carriers intact), lint at 2-finding baseline, prove PASS with uuid preserved — nothing silently dormant'
  - '832-side defect check: DISPROVED — pre-burst latest versions also had no in-file workflowMeta uuid, so the editor dropped nothing; no rail report warranted'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 4 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 4
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/999-Agentic-Engineering-Framework/.tasks/completed/T-2628-corpus-squash-burst-rca--operator-delete.md
generated_by: context-agent-hybrid
