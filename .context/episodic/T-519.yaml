# ============================================================================
# EPISODIC MEMORY - T-519: Fix do_vendor not found during fw doctor interactive init
# ============================================================================
# STATUS: ENRICHED
# Fixed function ordering bug — do_vendor defined after interactive init block that called it.

task_id: T-519
task_name: "Fix do_vendor not found during fw doctor interactive init"
completed: "2026-03-17T22:45:34Z"
workflow_type: build

summary: |
  Fixed bash function ordering bug — do_vendor was defined at line 1173 after the interactive init block at lines 100-200.

what_happened: |
  fw doctor's interactive init path called do_init → do_vendor, but do_vendor was defined
  later in bin/fw. This caused exit 127 (command not found). Fixed by reordering function
  definitions. Discovered during TermLink install testing alongside T-520 and T-521.

key_decisions: []

patterns_observed:
  - "Bash function ordering matters — functions must be defined before call sites in linear execution paths"
  - "TermLink install testing revealed 3 bugs (T-519/520/521) that manual testing missed"

artifacts:
  - "bin/fw"
  - "lib/init.sh"

tags: [bug, install, D2]

metrics:
  wall_clock_minutes: 6
  commits: 1
