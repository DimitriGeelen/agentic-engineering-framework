# ============================================================================
# EPISODIC MEMORY - T-3486: BVP loop S1: the outcomes ledger and cost rows — join dispatch token accounting to task ids, and record attributable:false rather than zero where attribution does not exist
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-25T22:45:17Z
# ============================================================================

task_id: T-3486
task_name: "BVP loop S1: the outcomes ledger and cost rows — join dispatch token accounting to task ids, and record attributable:false rather than zero where attribution does not exist"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-25T22:33:38Z
completed: 2026-09-25T22:44:58Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  BVP loop S1: the outcomes ledger and cost rows — join dispatch token accounting to task ids, and record attributable:false rather than zero where attribution does not exist

# Key outcomes
outcomes:
  - '`lib/bvp_outcomes.py` exists as a standalone module: appends rows to `.context/bvp-outcomes.jsonl`, reads `dispatches.jsonl` read-only, and imports nothing from `lib/bvp.sh`'
  - 'A task with dispatch rows gets a row carrying summed token cost and `attributable: true`, naming its source'
  - 'A task with **no** dispatch rows gets `attributable: false` with token fields **absent or null — never 0**, so an unmeasured task can never be averaged as a cheap one'
  - 'Multiple dispatch rows for one task sum rather than overwrite (a task dispatched twice cost both)'
  - 'The ledger is append-only: a second record for the same task adds a row and never rewrites the first, so history stays immutable (producer-not-judge)'
  - '`rubric_sha` is carried through from the task''s proposed score when present, so a row is always traceable to the rubric that produced its prediction'
  - 'A backfill over the existing corpus runs and reports the **attributable fraction**, because a calibration computed over the dispatched subset alone is biased and the bias must be visible in the output'
  - 'Tests pin all of the above against committed fixtures, including a **control leg** proving `attributable: false` is distinguishable from a genuine zero-cost measurement'
  - '`lib/bvp.sh` and `policy/value-drivers.yaml` are untouched by this task, verified with `git diff --name-only`'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 11 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 11
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/999-Agentic-Engineering-Framework/.tasks/completed/T-3486-bvp-loop-s1-the-outcomes-ledger-and-cost.md
generated_by: context-agent-hybrid
