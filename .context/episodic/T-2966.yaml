# ============================================================================
# EPISODIC MEMORY - T-2966: OBS id allocator recycles ids: next_id is max+1 over surviving inbox entries, so triaging the highest-numbered observation reuses its id
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-13T09:02:55Z
# ============================================================================

task_id: T-2966
task_name: "OBS id allocator recycles ids: next_id is max+1 over surviving inbox entries, so triaging the highest-numbered observation reuses its id"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-13T08:57:01Z
completed: 2026-08-13T09:02:40Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  OBS id allocator recycles ids: next_id is max+1 over surviving inbox entries, so triaging the highest-numbered observation reuses its id

# Key outcomes
outcomes:
  - '`next_id` in `agents/observe/observe.sh` derives the next OBS id from a persistent high-water mark, not from the max over surviving inbox entries, so triage cannot lower it'
  - 'The high-water mark is seeded from the true maximum ever issued (inbox entries AND ids referenced by existing task files), not just from what remains in `inbox.yaml`'
  - 'The live collision is resolved: the two observations currently sharing OBS-238 (T-2950''s `audit CTL-013 holds a third copy…` and this session''s Tier 0 note) no longer share an id, and the reassignment is recorded rather than silently overwritten'
  - 'Regression test proves the behaviour, not the arithmetic: issue an id, remove the highest entry as triage does, issue again, assert the second id differs from the first'
  - '`## RCA` records why a max-over-survivors allocator reads as correct — it always returns a well-formed unused-looking id — and what makes ids verifiable rather than merely plausible'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/999-Agentic-Engineering-Framework/.tasks/completed/T-2966-obs-id-allocator-recycles-ids-nextid-is-.md
generated_by: context-agent-hybrid
