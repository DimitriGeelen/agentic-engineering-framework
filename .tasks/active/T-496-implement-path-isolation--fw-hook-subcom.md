---
id: T-496
name: "Implement path isolation — fw hook subcommand + portable settings.json + doctor validation"
description: >
  From T-495 GO. Build: (1) fw hook subcommand in bin/fw — resolves FRAMEWORK_ROOT from symlink, PROJECT_ROOT from cwd, execs agents/context/$1.sh. (2) Update generate_claude_code_config() in lib/init.sh — use fw hook instead of hardcoded paths. (3) fw upgrade step 5 detects old-style hardcoded paths and regenerates. (4) fw doctor validates hook commands resolve to real executables. (5) E2E test phase for path isolation in self-test.

status: started-work
workflow_type: build
owner: agent
horizon: now
tags: [portability, enforcement, P0]
components: []
related_tasks: []
created: 2026-03-14T22:35:59Z
last_update: 2026-03-14T22:35:59Z
date_finished: null
---

# T-496: Implement path isolation — fw hook subcommand + portable settings.json + doctor validation

## Context

From T-495 GO (G-021). See `docs/reports/T-495-path-isolation-inception.md`.

## Acceptance Criteria

### Agent
- [x] `fw hook check-active-task` executes the correct gate script and returns proper exit code
- [x] `fw hook` with invalid name returns non-zero exit code (exit 2)
- [x] `generate_claude_code_config()` in lib/init.sh produces settings.json with `fw hook` commands (zero absolute paths)
- [x] `fw doctor` checks that hook commands resolve to real executables (check 6 + hook config)
- [x] `fw upgrade` step [5/8] detects old-style hardcoded paths and regenerates with `fw hook`
- [x] `fw help` shows `hook` subcommand
- [x] Settings.json generated by `fw init` contains zero absolute paths (quoted heredoc)

### Human
- [ ] [RUBBER-STAMP] Run `fw upgrade --dry-run` on termlink and verify it detects stale paths
  **Steps:**
  1. Run `fw upgrade --dry-run` in the termlink project
  2. Check step [5/8] output
  **Expected:** Shows "WOULD UPDATE" for hooks with stale paths
  **If not:** Note what step [5/8] reports

## Verification

# fw hook dispatches correctly
grep -q "hook)" bin/fw
# Settings.json has no hardcoded framework paths in hook commands
! grep -q "/agents/context/" .claude/settings.json || echo "FAIL: hardcoded paths remain"
python3 -c "import json; d=json.load(open('.claude/settings.json')); cmds=[h['command'] for hl in d['hooks'].values() for e in hl for h in e.get('hooks',[])]; assert all('fw hook' in c for c in cmds), f'Not all hooks use fw hook: {[c for c in cmds if \"fw hook\" not in c]}'; print(f'OK: {len(cmds)} hooks all use fw hook')"
# fw doctor checks hook resolution
grep -q "hook.*resolv\|hook.*valid\|hook.*path" bin/fw

## Decisions

<!-- Record decisions ONLY when choosing between alternatives.
     Skip for tasks with no meaningful choices.
     Format:
     ### [date] — [topic]
     - **Chose:** [what was decided]
     - **Why:** [rationale]
     - **Rejected:** [alternatives and why not]
-->

## Updates

### 2026-03-14T22:35:59Z — task-created [task-create-agent]
- **Action:** Created task via task-create agent
- **Output:** /opt/999-Agentic-Engineering-Framework/.tasks/active/T-496-implement-path-isolation--fw-hook-subcom.md
- **Context:** Initial task creation
